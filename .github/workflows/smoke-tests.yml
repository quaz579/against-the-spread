name: Playwright Smoke Tests

on:
  pull_request:
    branches: [ main, dev ]
    types: [opened, synchronize, reopened]

jobs:
  smoke-tests:
    name: Run Playwright Smoke Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Azure Functions Core Tools
        run: |
          wget -q https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb
          sudo dpkg -i packages-microsoft-prod.deb
          sudo apt-get update
          sudo apt-get install -y azure-functions-core-tools-4

      - name: Install Azurite
        run: npm install -g azurite

      - name: Restore dependencies
        run: dotnet restore

      - name: Build solution
        run: dotnet build --no-restore --configuration Release

      - name: Build Playwright test project
        run: dotnet build src/AgainstTheSpread.SmokeTests --no-restore --configuration Release

      - name: Install Playwright browsers
        run: |
          pwsh src/AgainstTheSpread.SmokeTests/bin/Release/net8.0/playwright.ps1 install --with-deps chromium

      - name: Run smoke tests
        run: dotnet test src/AgainstTheSpread.SmokeTests --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=test-results.trx"
        env:
          PWDEBUG: 0

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            **/TestResults/
            **/*.trx
          retention-days: 7

      - name: Upload Playwright traces
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-traces
          path: |
            **/test-results/
            **/*.zip
          retention-days: 7
